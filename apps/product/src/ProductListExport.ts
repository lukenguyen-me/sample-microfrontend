// This wrapper ensures the component is properly exported for Module Federation
// Using a wrapper component to ensure proper initialization
import ProductListWrapper from "./ProductListWrapper";

export default ProductListWrapper;
